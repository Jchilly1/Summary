<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      Neither a touchscreen nor Android TV's "leanback" launcher UI is required, so the
      app installs and shows up on both phones and TV/stick devices (Google TV boxes have
      no touchscreen at all).
    -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:name=".RewindApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.RewindVideoClub"
        android:usesCleartextTraffic="true">
        <!--
          usesCleartextTraffic is enabled because many home Plex servers are only
          reachable over plain http:// on the local network (e.g. http://192.168.x.x:32400).
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.RewindVideoClub">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
